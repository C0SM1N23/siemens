\documentclass[11pt,openany]{report}

\newcommand{\docrevision}{3.0}
\newcommand{\docdate}{22 August 2026}
\newcommand{\docbaseline}{branch \texttt{RISCV}, at \texttt{d534045}}

\input{preamble}

\begin{document}

% ===========================================================================
\begin{titlepage}
\centering
\vspace*{22mm}

{\color{accent}\rule{\linewidth}{1.2pt}}\\[6mm]
{\Huge\bfseries RV32I System-on-Chip}\\[4mm]
{\LARGE CPU, Programmable Interrupt Controller\\[2mm] and Machine Timer}\\[6mm]
{\Large\itshape RTL Implementation Documentation}\\[4mm]
{\color{accent}\rule{\linewidth}{1.2pt}}

\vfill

\begin{tabular}{@{}lL{0.58\linewidth}@{}}
\toprule
Document        & RV32I SoC --- RTL Implementation Documentation \\
Revision        & \docrevision \\
Date            & \docdate \\
Source baseline & \docbaseline \\
Target          & Behavioural RTL simulation (no vendor IP) \\
Language        & Verilog--2005 (IEEE 1364--2005) \\
Deliverables    & \file{hdl/}, \file{debug/} (testbenches, SVA, coverage) \\
\bottomrule
\end{tabular}

\vspace{12mm}

\begin{minipage}{0.86\linewidth}
\small
This document describes the RTL as implemented on the source baseline named above.
Every register address, bit field, reset value and state transition in it was read
back from the source files or from the elaborated design; where the feature
specifications and the delivered RTL differ, the difference is stated rather than
smoothed over.
\end{minipage}

\vspace{18mm}
\end{titlepage}

% ===========================================================================
\pagenumbering{roman}
\setcounter{page}{1}

\chapter*{Revision history}
\addcontentsline{toc}{chapter}{Revision history}
\markboth{Revision history}{}

\begin{tabular}{@{}lL{0.16\linewidth}L{0.60\linewidth}@{}}
\toprule
\textbf{Rev.} & \textbf{Date} & \textbf{Change} \\
\midrule
1.0 & 17 August 2026 & First issue. The CPU, PIC and machine timer as delivered
on baseline \texttt{95d1e29}, with the traceability matrix and the verification
evidence available then. \\
\addlinespace
2.0 & 20 August 2026 & Two RTL changes: every flop is now reset, and the
reserved bits of \reg{SRCx\_CONFIG} and \reg{ESCALATION\_CFG} are now hardwired
to zero, as the register map had always said. New chapters on the compile order,
on the reset architecture with every register's reset value, and on the
verification procedures step by step. Regression: six runs to twelve. \\
\addlinespace
3.0 & \docdate & No hardware change.
\emph{Added:} Part~\ref{part:verif}, verification and validation, with
Section~\ref{sec:valscope} stating how much of each value space each area
covers; two block benches, \file{tb\_bp} for the branch predictor and
\file{tb\_alu} for the arithmetic path, taking the regression to fourteen runs
and 2\,520 checks; Section~\ref{sec:delegated}, the matrix of design questions
the two briefs delegated.
\emph{Corrected:} the pipeline and command-latch payloads, described as unreset
since revision 1.0; the trap-cause table, which listed the causes in numeric
rather than evaluation order; what the decoder does with an illegal encoding;
the PIC register count, 56 and not 59; every check count, now read from the
regression log.
\emph{Removed:} the file inventory duplicated in Appendix~\ref{app:modules}, and
the compile-order rationale, cut to what a maintainer needs. \\
\bottomrule
\end{tabular}

\chapter*{Scope and reading guide}
\addcontentsline{toc}{chapter}{Scope and reading guide}
\markboth{Scope and reading guide}{}

Three blocks are documented here: the RV32I CPU, the programmable interrupt
controller, and the machine timer. They are delivered together because the
interrupt path only makes sense as a whole, but the PIC is a self-contained IP
block with its own register interface and is documented as one
(Part~\ref{part:pic}).

Three other blocks appear in the surrounding system --- a multi-channel DMA
engine, a dual-port SRAM, and the interconnect --- and are \emph{not} part of this
delivery. They are described only where the CPU or PIC has to interoperate with
them, and nothing in this document should be read as documenting them.

\paragraph{How statements are backed.} Requirements and design intent come from
the two feature specifications and the project's own requirement tags
(\texttt{REQ1}--\texttt{REQ12}, \texttt{D1}--\texttt{D27}, and the PIC's
\texttt{D-*} tags), all of which also appear as comments in the source. Behaviour
comes from the RTL. Where a section states what the hardware does, the file it was
read from is named in the margin or in the text. Verification results come from
the two simulation flows and are quoted only where a run produced them.

\paragraph{Implemented, and validated, are separate claims.} This document keeps
them apart deliberately. Chapters~\ref{ch:cpuarch} to \ref{ch:mtimer} describe
what was built and were checked line by line against the source of the baseline
named on the title page. Part~\ref{part:verif} describes what was demonstrated,
and Section~\ref{sec:valscope} states how much of each value space that
demonstration actually reached --- which spaces were swept exhaustively, which
were sampled at their boundaries, and which were exercised only by whatever
values the directed program happened to use. Where the evidence for a behaviour
is thinner than for its neighbours, the row or paragraph says so.

\paragraph{What changed in revision 3.0.} No hardware changed. The document was
reviewed against the RTL and against the regression log, and the statements that
no longer matched were corrected; the largest of those was the description of
pipeline and command-latch payloads as unreset, which stopped being true when
revision 2.0 made the reset policy absolute. Two behaviours that were
implemented and documented but never directly exercised --- the branch
predictor's reset state and its return-address-stack boundaries, and the ALU's
decode and operand boundaries --- were closed with two new benches rather than
left described as though they had been verified. Both are recorded in
Sections~\ref{sec:vp-bp} and \ref{sec:vp-alu}, together with the mutation that
was used to show each bench can fail.

\paragraph{Assumed background.} The reader is expected to know RV32I and normal
RTL practice; the base instruction set is not re-explained here. AXI4-Lite
\emph{is} explained, in Section~\ref{sec:axiprimer}, because the bus is where the
integration surface of these blocks lies and because the rest of the document
refers constantly to its handshake rules. That section covers the protocol only
as far as is needed to follow what this implementation does with it.

\tableofcontents
\listoffigures
\listoftables

\cleardoublepage
\pagenumbering{arabic}
\setcounter{page}{1}

% ===========================================================================
\part{System overview}\label{part:system}
\input{chapters/system/soc_overview}

\part{RISC-V CPU subsystem}\label{part:cpu}
\input{chapters/cpu/architecture}
\input{chapters/cpu/pipeline_control}
\input{chapters/cpu/fetch_bp}
\input{chapters/cpu/lsu_axi}
\input{chapters/cpu/csr_traps}

\part{Programmable interrupt controller}\label{part:pic}
\input{chapters/pic/architecture}
\input{chapters/pic/registers}
\input{chapters/pic/behaviour}

\part{SoC integration}\label{part:integration}
\input{chapters/integration/integration}
\input{chapters/integration/mtimer}

\part{Verification and validation}\label{part:verif}
\input{chapters/reference/verification}
\input{chapters/reference/verif_plan}

\part{Engineering reference}\label{part:reference}
\input{chapters/reference/reset}
\input{chapters/reference/fsm_summary}
\input{chapters/reference/traceability}
\input{chapters/reference/build_order}
\input{chapters/reference/limitations}

\appendix
\input{appendices/regmap}
\input{appendices/modules}
\input{appendices/acronyms}

\bibliographystyle{plain}
\begin{thebibliography}{9}
\bibitem{riscv-unpriv}
RISC-V Foundation, \emph{The RISC-V Instruction Set Manual, Volume I:
Unprivileged ISA}, version 20191213.

\bibitem{riscv-priv}
RISC-V Foundation, \emph{The RISC-V Instruction Set Manual, Volume II:
Privileged Architecture}, version 20211203.

\bibitem{axi}
ARM Limited, \emph{AMBA AXI and ACE Protocol Specification}, IHI 0022E.
\end{thebibliography}
\addcontentsline{toc}{chapter}{Bibliography}

\end{document}
