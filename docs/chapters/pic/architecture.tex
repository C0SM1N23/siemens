\chapter{PIC architecture}
\label{ch:picarch}

\section{Block boundary}

\file{hdl/pic.v} is a self-contained IP block. Its interface is 16 hardware
interrupt lines, the four-signal CPU handshake, and one AXI4-Lite slave port for
configuration and status. It holds no CPU state and makes no assumption about the
CPU beyond the claim/end-of-interrupt protocol.

The feature specification for this block was explicitly a draft that delegated
the ``non-standard design challenges'' to the implementer. The choices recorded
in Chapter~\ref{ch:picbehav} therefore \emph{are} the feature specification for
those areas.

\begin{table}[H]
\centering
\caption{PIC top-level interface (\file{hdl/pic.v})}
\label{tab:picif}
\begin{tabular}{@{}llL{0.48\linewidth}@{}}
\toprule
\textbf{Signal} & \textbf{Width} & \textbf{Function} \\
\midrule
\sig{irq\_src\_i}      & 16 & Hardware interrupt lines, level- or edge-triggered per source. \\
\sig{cpu\_irq\_o}      & 1  & Registered level request to the CPU. \\
\sig{cpu\_irq\_vec\_o} & 4  & Identifier of the source being offered. \\
\sig{cpu\_irq\_ack\_i} & 1  & Claim pulse from the CPU. \\
\sig{cpu\_irq\_eoi\_i} & 1  & End-of-interrupt pulse from the CPU. \\
\sig{s\_axi\_*\_i} / \sig{\_o} & -- & AXI4-Lite slave, all five channels. \\
\bottomrule
\end{tabular}
\end{table}

Two internal sizes are fixed by localparam: \sig{NSRC} = 16 logical source slots,
and \sig{MAXNEST} = 16, the physical depth of the nesting stack.

\section{Source model}

Each of the 16 slots carries a hardware line and a software channel, giving 32
logical sources feeding one resolution pipeline. The slot request is
(\file{hdl/pic.v}):

\begin{center}
\ttfamily\small
hw\_req = cfg\_trig[s] ? edge\_pend[s] : irq\_src\_i[s];\\
req[s]  = (hw\_req | sw\_pend[s]) \& int\_enable[s];
\end{center}

A software request merges into the same slot, so once inside the pipeline it is
indistinguishable from a hardware one --- same priority, same deadline, same
nesting behaviour.

\section{Resolution pipeline}

\begin{figure}[H]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[node distance=6mm]
  \node[blkf,minimum width=30mm,align=left] (slot)
    {\textbf{per slot, $\times$16}\\[2pt]
     \scriptsize level / edge detect\\
     \scriptsize $\vert$ software channel\\
     \scriptsize \& INT\_ENABLE\\[3pt]
     \scriptsize deadline counter\\
     \scriptsize $\to$ effective band};

  \node[blk,right=16mm of slot,minimum width=34mm,align=left] (key)
    {\textbf{priority key}\\[2pt]
     \scriptsize \{band\_urgency[1:0],\\
     \scriptsize \ \ INTRA[3:0], 15$-$index\}\\
     \scriptsize 10 bits, higher = more urgent};

  \node[blk,right=16mm of key,minimum width=32mm,align=left] (res)
    {\textbf{resolver}\\[2pt]
     \scriptsize eligible = req \& !active\\
     \scriptsize \& key $>$ top\_key\\
     \scriptsize masked if depth = NEST\_MAX};

  \node[blk,below=13mm of res,minimum width=32mm,align=left] (reg)
    {\textbf{output register}\\[2pt]
     \scriptsize cpu\_irq\_o $\leftarrow$ offer\_val\\
     \scriptsize cpu\_irq\_vec\_o $\leftarrow$ res\_id};

  \node[blkf,below=13mm of key,minimum width=34mm,align=left] (stack)
    {\textbf{nesting stack}\\[2pt]
     \scriptsize \{key, id\} pairs, depth 0..NEST\_MAX\\
     \scriptsize push on ack, pop on eoi};

  \node[blkf,below=13mm of slot,minimum width=30mm,align=left] (axi)
    {\textbf{axi\_lite\_slave}\\[2pt]
     \scriptsize register file\\
     \scriptsize 56 mapped words};

  \draw[flow] (slot) -- (key);
  \draw[flow] (key) -- (res);
  \draw[flow] (res) -- (reg);
  \draw[flow] (stack) -- node[lbl,right]{top\_key} (res);
  \draw[flow] (axi) -- node[lbl,above]{config} (slot);
  \draw[flow,<->] (axi) -- node[lbl,above]{status} (stack);
  \draw[flow] (reg.east) -- ++(9mm,0) node[lbl,right,align=left]{\scriptsize to CPU};
  \draw[flow] ($(stack.south)+(0,-1mm)$) ++(0,-4mm) -- (stack.south)
        node[lbl,below=5mm,align=center]{\scriptsize ack\_i / eoi\_i};
  \draw[flow] (slot.west) ++(-9mm,0) -- (slot.west) node[lbl,left=9mm]{\scriptsize irq\_src\_i};
\end{tikzpicture}}

\caption{PIC internal structure. The resolver is entirely combinational; the only
registered stage on the request path is the output pair.}
\label{fig:picblock}
\end{figure}

\subsection{Priority key}

Every source is reduced to a single 10-bit unsigned number and the resolver picks
the maximum:

\begin{center}
\ttfamily\small
key[s] = \{ band\_urg(eff\_band[s], band\_cfg), cfg\_intra[s], 15 - s \};
\end{center}

Band urgency dominates, then intra-band priority, then the last field. Because
that field is $15-\text{index}$, it is larger for smaller indices, so a tie is
won by the lowest-numbered source. The ordering is total: there is never an
ambiguous case and the winner is always unique.

Note what is \emph{not} in the key: the band \emph{number} carries no ranking of
its own. A band's urgency is looked up from \reg{BAND\_CONFIG}, which is what
lets software reorder all four bands with one register write.

\subsection{Eligibility and preemption}

\begin{center}
\ttfamily\small
if (req[i] \&\& !active[i] \&\& (!has\_active || key[i] > top\_key))
\end{center}

A source is eligible if it has a request, is not already in service, and --- when
anything is in service --- its key is \emph{strictly} greater than the key on top
of the nesting stack.

That single strict comparison does two jobs. It is the preemption rule: a more
urgent source displaces a less urgent handler. And it is the re-entrancy
guarantee: a source cannot re-offer itself, because its own key is not strictly
greater than itself.

\subsection{Output registration}

\sig{cpu\_irq} and \sig{cpu\_irq\_vec} are registered from the same resolver
output on the same clock edge, so the pair the CPU sees is always
self-consistent. The cost is one cycle of latency; the benefit is that the PIC
never sits on the combinational timing path of the source lines. \sig{cpu\_irq\_vec}
holds its last presented value while idle.

A second register, \sig{cpu\_irq\_vec\_d}, holds the vector as presented in the
previous cycle. Claims are attributed to \emph{that} copy, because
\sig{cpu\_irq\_ack} arrives one cycle after the CPU sampled the vector it acted
on. This keeps the PIC's notion of the claimed source aligned with the CPU's
latched \reg{mcause} even if a more urgent source appeared in the meantime.


\subsection{How a request is chosen, end to end}
\label{sec:reqchosen}

The three subsections above describe the pieces. This one puts them in order, as
the hardware evaluates them, every clock cycle, for all 16 slots at once. Nothing
here is sequential: steps 1 to 4 are one combinational cone, and only step 5 is a
register.

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=4.2mm]
  \node[blkf,text width=108mm,align=left] (s1)
    {\textbf{1. Build the request, per slot $s$} \hfill \footnotesize combinational\\[2pt]
     \ttfamily\footnotesize hw\_req = TRIG[s] ? edge\_pend[s] : irq\_src\_i[s];\\
     \ttfamily\footnotesize req[s]\ \ = (hw\_req | sw\_pend[s]) \& INT\_ENABLE[s];\\[2pt]
     \footnotesize Hardware and software requests merge here, so past this point
     they are indistinguishable. \reg{INT\_ENABLE} is the first of the two masks.};
  \node[blkf,below=of s1,text width=108mm,align=left] (s2)
    {\textbf{2. Build the priority key, per slot} \hfill \footnotesize combinational\\[2pt]
     \ttfamily\footnotesize key[s] = \{ band\_urg(eff\_band[s], BAND\_CONFIG), INTRA[s], 15-s \};\\[2pt]
     \footnotesize \sig{eff\_band} is the \emph{escalated} band, not the configured
     one, which is how a missed deadline changes the outcome without changing
     configuration. The $15-s$ field makes the ordering total.};
  \node[blkf,below=of s2,text width=108mm,align=left] (s3)
    {\textbf{3. Filter to the eligible slots} \hfill \footnotesize combinational\\[2pt]
     \ttfamily\footnotesize eligible[s] = req[s] \&\& !active[s] \&\&\\
     \ttfamily\footnotesize \ \ \ \ \ \ \ \ \ \ \ \ \ \ (!has\_active || key[s] > top\_key);\\[2pt]
     \footnotesize \sig{!active[s]} is in-service suppression. The strict
     comparison against the top of the nesting stack is both the preemption rule
     and the re-entrancy guarantee: a source is never strictly greater than
     itself.};
  \node[blkf,below=of s3,text width=108mm,align=left] (s4)
    {\textbf{4. Take the maximum key among the eligible} \hfill \footnotesize combinational\\[2pt]
     \footnotesize A linear scan over the 16 slots yields
     \sig{res\_id}, \sig{res\_key} and \sig{res\_val}. The winner is unique
     because the key ordering is total.};
  \node[blkf,below=of s4,text width=108mm,align=left] (s5)
    {\textbf{5. Apply the depth limit} \hfill \footnotesize combinational\\[2pt]
     \ttfamily\footnotesize offer\_val\ \ = res\_val \&\& (depth <\ NEST\_MAX);\\
     \ttfamily\footnotesize depth\_block = res\_val \&\& (depth >= NEST\_MAX);\\[2pt]
     \footnotesize The limit is enforced by \emph{withholding the offer}, so the
     CPU is never shown an interrupt it could not legally claim. A blocked
     preemption is visible, not silent: it sets \reg{INT\_STATUS.OVF} and
     \reg{NEST\_STATUS.OVF}, and the source stays pending.};
  \node[blk,below=of s5,text width=108mm,align=left] (s6)
    {\textbf{6. Register the offer} \hfill \footnotesize one clock edge\\[2pt]
     \ttfamily\footnotesize cpu\_irq\_o <= offer\_val;\\
     \ttfamily\footnotesize if (offer\_val) cpu\_irq\_vec\_o <= res\_id;\\[2pt]
     \footnotesize Both from the same edge, so the pair the CPU sees is always
     self-consistent, and the PIC never sits on the combinational path of the
     source lines.};
  \node[blk,below=of s6,text width=108mm,align=left] (s7)
    {\textbf{7. The CPU applies its own mask} \hfill \footnotesize in \file{cpu\_top.v}\\[2pt]
     \ttfamily\footnotesize irq\_take = valid \&\& !lsu\_active \&\& mstatus.MIE \&\&\\
     \ttfamily\footnotesize \ \ \ \ \ \ \ \ \ \ \ cpu\_irq \&\& mie[16 + cpu\_irq\_vec];\\[2pt]
     \footnotesize The second of the two masks, and the instruction-boundary
     condition. \reg{INT\_ENABLE} and \reg{mie} are deliberately independent:
     ``this source may reach the CPU'' and ``current software cares'' are
     different questions.};
  \foreach \x/\y in {s1/s2,s2/s3,s3/s4,s4/s5,s5/s6,s6/s7} \draw[flow] (\x) -- (\y);
\end{tikzpicture}
\caption{How a request is chosen. Steps 1--5 are one combinational cone
evaluated every cycle for all 16 slots; step 6 is the only register on the
path; step 7 happens in the CPU, not the controller.}
\label{fig:reqflow}
\end{figure}

\begin{table}[H]
\centering
\caption{Where each step can stop a request, and what makes it visible}
\label{tab:reqstops}
\begin{tabular}{@{}cL{0.34\linewidth}L{0.36\linewidth}@{}}
\toprule
\textbf{Step} & \textbf{Reason a request stops here} & \textbf{How software sees it} \\
\midrule
1 & the source is not enabled in \reg{INT\_ENABLE} &
    \reg{SRCx\_STATUS.PEND} stays 0 \\
3 & the source is already in service &
    \reg{SRCx\_STATUS.ACTIVE} = 1 \\
3 & its key is not strictly above the top of the nesting stack &
    \reg{SRCx\_STATUS.PEND} = 1 with \reg{NEST\_STATUS.DEPTH} $>$ 0 \\
4 & another source has a higher key &
    both read \reg{PEND} = 1; \reg{ACTIVE\_VEC} names the winner \\
5 & the nesting depth limit is reached &
    \reg{INT\_STATUS.OVF} and \reg{NEST\_STATUS.OVF} \\
7 & masked in \reg{mie}, or \reg{mstatus.MIE} = 0, or an access is in flight &
    \reg{mip}[16+vec] = 1 while no trap is taken \\
\bottomrule
\end{tabular}
\end{table}

\begin{deviation}
\textbf{The CPU mask takes part in the resolution.} A source enabled in
\reg{INT\_ENABLE} but masked in \reg{mie} used to stop at step 7, \emph{after}
winning step 4: it stayed the controller's standing offer and starved every less
urgent source. The core now hands \reg{mie}[31:16] to the controller as
\sig{cpu\_mask\_i} and step 4 skips a masked source, so the two masks compose
instead of deadlocking. A masked source is still pending --- it appears in
\sig{pending\_o} and therefore in \reg{mip}, and its deadline counter keeps
running --- it is simply not offered until the mask lifts. The channel sweep in
the system program leaves such a source enabled and asserted for the whole run
and checks that the channels behind it are still served.
\end{deviation}

\section{Per-source lifecycle}

The PIC has no state register per source. The lifecycle below is encoded in
separate flag arrays --- \sig{edge\_pend}, \sig{sw\_pend}, \sig{active},
\sig{escalated}, \sig{spurious}. The diagram describes the behaviour those flags
produce.

\begin{figure}[H]
\centering
\begin{tikzpicture}[x=1mm,y=1mm,>={Stealth[length=2.4mm]},semithick]
  \node[stbi] (idle) at (0,22)   {IDLE};
  \node[stb]  (pend) at (62,22)  {PENDING};
  \node[stb]  (act)  at (126,22) {ACTIVE};
  \node[stb]  (esc)  at (62,-26) {ESCALATED};

  % idle -> pending
  \draw[->,draw=accent] (idle) -- node[lbl,above,align=center]
    {line, latched edge or software\\\scriptsize trigger, and enabled} (pend);

  % pending -> active, two ways
  \draw[->,draw=accent] (pend) to[bend left=20]
    node[lbl,above,align=center,pos=0.5]{cpu\_irq\_ack\_i (claim, push)\\
     \scriptsize effective band and deadline\\
     \scriptsize counter revert to the config} (act);
  \draw[->,draw=accent] (pend) to[bend right=20]
    node[lbl,below,align=center,pos=0.5]
    {request gone before the claim\\\scriptsize $\to$ SPURIOUS, still pushed} (act);

  % active -> idle, routed above every other label
  \draw[->,draw=accent] (act.north) -- (126,58) -- (0,58) -- (idle.north);
  \node[lbl,above=0.5mm,align=center] at (63,58)
    {cpu\_irq\_eoi\_i (pop) --- the slot may be offered again};

  % pending <-> escalated
  \draw[->,draw=accent] ($(pend.south)+(-8,0)$) -- ($(esc.north)+(-8,0)$)
    node[lbl,midway,left=1mm,align=right]{ddl\_cnt reaches\\\scriptsize DEADLINE};
  \draw[->,draw=accent] ($(esc.north)+(8,0)$) -- ($(pend.south)+(8,0)$)
    node[lbl,midway,right=1mm,align=left]{still pending,\\\scriptsize higher effective band};

  \draw[->,draw=accent] (esc) to[out=-125,in=-55,looseness=4]
    node[lbl,below=1mm,align=center]{MULTI = 1: counter re-arms, escalate again} (esc);
\end{tikzpicture}
\caption{Per-source lifecycle. Flag-encoded, not a state register: a source can
be pending and escalated at the same time, and escalation only modifies the
effective band used to build the priority key. Escalation is undone at the
\emph{claim} --- the counter and the effective band track the configuration
again as soon as the slot stops being a pending request awaiting service.}
\label{fig:picsrcfsm}
\end{figure}

\section{Nesting stack}

The stack holds the \{key, id\} pair of every source currently in service:

\begin{center}
\ttfamily\small
reg [3:0] stack\_id  [0:MAXNEST-1];\\
reg [9:0] stack\_key [0:MAXNEST-1];\\
reg [4:0] depth;
\end{center}

\sig{depth} is the stack pointer; the top is \sig{top\_idx} = \sig{depth} $-$ 1,
valid whenever \sig{depth} is non-zero. All three reset to zero, so
\reg{NEST\_STATUS} and \reg{ACTIVE\_VEC} read their documented reset values on
the first access after release.

Storing the key, not just the id, is what makes the preemption threshold stable:
reconfiguring a source's band while it is in service cannot corrupt nesting,
because the threshold in use is the snapshot taken at the claim. New pending
evaluations of that source use the new band immediately.

\begin{figure}[H]
\centering
\begin{tikzpicture}[>={Stealth[length=2.4mm]},semithick,node distance=26mm]
  \node[stbi] (d0) {depth 0};
  \node[stb,right=24mm of d0] (d1) {depth 1};
  \node[stb,right=24mm of d1] (dn) {depth $n$};
  \node[stb,right=24mm of dn] (dmax) {NEST\_MAX};

  \draw[->,draw=accent] (d0) to[bend left=14] node[lbl,above]{ack: push} (d1);
  \draw[->,draw=accent] (d1) to[bend left=14] node[lbl,below]{eoi: pop} (d0);
  \draw[->,draw=accent,dashed] (d1) to[bend left=14] node[lbl,above]{ack} (dn);
  \draw[->,draw=accent,dashed] (dn) to[bend left=14] node[lbl,below]{eoi} (d1);
  \draw[->,draw=accent] (dn) to[bend left=14] node[lbl,above]{ack} (dmax);
  \draw[->,draw=accent] (dmax) to[bend left=14] node[lbl,below]{eoi} (dn);
  \draw[->,draw=accent,loop above] (dmax) to
     node[lbl,above,align=center]{offers masked,\\\scriptsize INT\_STATUS.OVF set} (dmax);
\end{tikzpicture}
\caption{Nesting depth. The limit is enforced by masking offers, so a claim that
would exceed it is never presented to the CPU in the first place.}
\label{fig:picdepth}
\end{figure}

\section{Register interface}

The AXI4-Lite handshake is not implemented in the PIC. It instantiates
\file{hdl/axi\_lite\_slave.v}, the shared slave used by both the PIC and the
machine timer (Section~\ref{sec:axislave}), and supplies only the register file:
a combinational read multiplexer, a one-cycle write pulse, and two
combinational qualifiers \sig{rd\_ok} / \sig{wr\_ok} that mark which offsets are
readable and writable. Everything else --- unmapped offsets, writes to read-only
registers --- becomes SLVERR through that module.
